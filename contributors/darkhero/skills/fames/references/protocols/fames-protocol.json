{
  "schema": 2,
  "id": "FAMES",
  "version": "1.18.0",
  "updated": "2026-08-18",
  "full_en": "First-principles Adaptive Minimal-token Evidence-aligned Self-consistency",
  "purpose": "Fleet-wide umbrella and standalone trigger for the complete FP, MTM, SCF, AEX, and SEAL contract.",
  "legacy_umbrella_aliases": [
    "AEX Stack"
  ],
  "narrow_terms": {
    "AEX": "Cross-cycle adaptation only; AEX remains the narrow evolution loop.",
    "AEX Loop": "The concrete EXEC-INSTR-DISTILL-PROMOTE-OPEN-GUARD evolution cycle."
  },
  "letters": {
    "F": {
      "concept": "FP",
      "meaning": "First-principles"
    },
    "A": {
      "concept": "AEX",
      "meaning": "Adaptive evolution"
    },
    "M": {
      "concept": "MTM",
      "meaning": "Minimal-token mechanism"
    },
    "E": {
      "concept": "SEAL",
      "meaning": "Evidence-aligned completion"
    },
    "S": {
      "concept": "SCF",
      "meaning": "Self-consistent residual"
    }
  },
  "execution_order": [
    "FP",
    "MTM",
    "SCF",
    "AEX",
    "SEAL"
  ],
  "canonical_protocols": {
    "FP": "_registry/goal-vector-protocol.json",
    "MTM": "_registry/mtm-protocol.json",
    "SCF": "_registry/scf-protocol.json",
    "AEX": "_registry/fleet-token-ladder.json",
    "SEAL": "_registry/seal-protocol.json"
  },
  "trigger": {
    "tokens": [
      "FAMES",
      "$fames"
    ],
    "standalone_means": "Execute and account for the complete five-phase contract.",
    "does_not_expand_authority": true,
    "required_phase_states": [
      "PASS",
      "NOT_APPLICABLE",
      "UNKNOWN",
      "FAIL"
    ],
    "not_applicable_rule": "Allowed only when the phase activation predicate is demonstrably false; cite the evidence.",
    "unknown_rule": "Missing, stale, mismatched, exceptional, or unverifiable evidence is UNKNOWN and fails closed."
  },
  "task_adaptation": {
    "profiles": {
      "read-only explanation": "R0",
      "diagnosis": "R0",
      "one-shot deterministic edit": "R1",
      "multi-file implementation": "R1",
      "live-device mutation": "R2",
      "deployment/external write": "R2",
      "recurring autonomous control": "R2",
      "safety-critical operation": "R3",
      "cross-cycle optimization": "R1",
      "distributed/fleet promotion": "R2"
    },
    "rule": "A profile changes phase depth, evidence classes, and tool budgets only; it never expands user authority.",
    "risk_classes": {
      "R0": ["read_only", "no_external_state", "no_secret", "no_live_mutation"],
      "R1": ["reversible_local_edit", "deterministic_tests", "no_privileged_or_external_write"],
      "R2": ["fresh_state", "rollback", "read_back", "authority_verification"],
      "R3": ["fail_closed", "explicit_authorization", "transaction_journal", "recovery_drill"]
    }
  },
  "goal_identity": {
    "fields": ["outcome", "verification", "constraints", "authority_scope", "non_goals", "irreversible_boundary", "success_horizon", "task_profile", "required_evidence_classes"],
    "semantic_hash": "sha256 of canonical UTF-8 JSON with sorted keys and compact separators",
    "success_horizons": ["one-shot", "session", "recurring", "cross-cycle"]
  },
  "complexity_budget": {
    "fields": ["new_files", "new_dependencies", "new_resident_processes", "new_abstractions", "duplicated_authorities"],
    "over_budget_rule": "Evidence-backed justification is mandatory; otherwise convergence fails."
  },
  "residual_vector": {
    "fields": ["R_outcome", "R_safety", "R_evidence", "R_complexity", "R_portability", "R_authority", "R_operability"],
    "non_compensable": ["R_safety", "R_evidence", "R_authority"]
  },
  "authority_discipline": {
    "canonical_writer_count": 1,
    "legacy_writer_states": ["disabled", "diet-refused", "read-only", "mechanically-gated"],
    "lock_is_not_authority": true,
    "adapter_must_not_decide_policy": true,
    "traceable_roles": ["policy", "apply", "measure", "promote", "restore"]
  },
  "transaction_contract": {
    "activation": ["R2", "R3"],
    "states": ["PREPARE", "APPLY", "VERIFY", "COMMITTED", "RECOVERED", "UNKNOWN"],
    "required_fields": ["before_state", "rollback", "intended_mutation_identity", "read_back", "authority_verified", "journal_state", "recovery"],
    "r3_extra": "recovery_drill must PASS",
    "interruption_rule": "A transaction not COMMITTED or RECOVERED is UNKNOWN and fails closed."
  },
  "evidence_contract": {
    "required_fields": ["goal_identity", "result_identity", "timestamp", "freshness_seconds", "source", "method", "authority", "state_before", "state_after", "exit_status", "error_category", "diagnostic"],
    "forbidden_material": ["raw credential", "token", "cookie", "SCRAM material", "URL credential", "unbounded exception dump"],
    "retention": "bounded by producer policy or janitor",
    "live_state_rule": "stale evidence cannot support a live-state claim"
  },
  "machine_validation": {
    "command": "python scripts/fames_fleet.py validate-run --input <run.json> --json",
    "input": "provider-neutral JSON phase ledger",
    "output": "JSON plus deterministic exit code",
    "phase_order": ["FP", "MTM", "SCF", "AEX", "SEAL"],
    "aex_activation": "Only a verified comparable cross-cycle residual activates AEX.",
    "unknown_exit": "non-zero"
  },
  "self_check": {
    "purpose": "FAMES run against FAMES. Each case states its expectation before it runs (FP), costs one in-process call and zero model tokens (MTM), its failure count IS the residual (SCF), a non-zero dimension is the only thing that activates a change (AEX), and the written record cites package_sha and replays (SEAL).",
    "command": "python scripts/fames_fleet.py self-check --workspace <root> --json",
    "cases_ssot": "_registry/fames-cases.json",
    "cases_bundled": "references/cases.json",
    "evidence_dir": "_registry/fames-evidence/self",
    "residual_keys": ["R_CONTRACT", "R_SEMANTICS", "R_FLEET", "R_CAPABILITY", "R_HYGIENE", "R_FRESHNESS"],
    "fail_modes": {
      "closed": "blocks; the run is not ok",
      "degraded": "counted into the residual and reported, but does not block"
    },
    "unknown_rule": "UNKNOWN blocks regardless of the declared fail_mode, including an unknown case kind, residual dimension, or fail mode. Data cannot introduce behaviour.",
    "extension_rule": "Adding a check means adding a case to the SSOT, not writing code. Cases are evaluated in-process by declared kind; no case may name a command to execute.",
    "honesty_rule": "A real residual stays red until it is fixed. Silencing or deleting a case to make a run green is a contract violation, not a fix.",
    "quota_cost": {"metered_calls": 0, "model_tokens_external": 0},
    "trust_class": "measured"
  },
  "phase_contract": {
    "FP": {
      "owns": [
        "Outcome",
        "Verification",
        "Constraints",
        "semantic_goal_hash"
      ],
      "must_not_own": [
        "execution_loop"
      ]
    },
    "MTM": {
      "owns": [
        "bounded_route",
        "read_budget",
        "receipt_reuse"
      ],
      "must_not_own": [
        "completion_truth"
      ]
    },
    "SCF": {
      "activation": "A verified result exists for the current goal identity.",
      "owns": [
        "goal_result_residual"
      ],
      "must_not_own": [
        "evidence_collection"
      ]
    },
    "AEX": {
      "activation": "A verified comparable cross-cycle residual remains.",
      "owns": [
        "cross_cycle_adaptation"
      ],
      "must_not_own": [
        "one_shot_execution"
      ]
    },
    "SEAL": {
      "owns": [
        "completion_predicate",
        "evidence_freshness",
        "identity_alignment",
        "graph_closure"
      ],
      "must_not_own": [
        "another_evolution_loop"
      ]
    }
  },
  "completion_predicate": {
    "goal_hash_matches": true,
    "required_evidence_is_fresh_and_passing": true,
    "active_work_graph_is_closed": true,
    "all_phase_states_are_terminal": true,
    "accepted_terminal_states": [
      "PASS",
      "NOT_APPLICABLE"
    ]
  },
  "freshness": {
    "rule": "FAMES is resolved at run time from disk, never from the contract text already sitting in a thread's context.",
    "generation_stamp": "SKILL.md carries a FAMES-GEN line; the runtime command reports the on-disk value.",
    "runtime_command": "python _skill/fleet-skills/fames/scripts/fames_fleet.py status --json --workspace %AI_WORKSPACE%",
    "stale_context_rule": "A FAMES-GEN in context that differs from the on-disk skill_gen means the held copy is stale: re-read SKILL.md and the bundled protocols from disk, and treat any phase already judged with the stale copy as UNKNOWN.",
    "parity_rule": "Bundled protocols must match the registry SSOT by version and by line-ending-normalized content; drift is UNKNOWN and fails closed. A machine with no registry is a legitimate cold load and reports NOT_APPLICABLE.",
    "parity_engine": "_skill/fleet-skills/fames/scripts/fames_fleet.py",
    "applies_to": [
      "threads opened before the contract changed",
      "threads opened after the contract changed"
    ]
  },
  "canonical_skill": "fames",
  "architecture_standard": {
    "provider_neutral": true,
    "single_canonical_skill": true,
    "provider_surfaces": "data-driven junctions only",
    "platform_neutral_execution": {
      "rule": "Canonical policy selects execution surfaces only by capability, availability, authority, safety, evidence freshness, and cost. It never branches on an AI host, model, vendor, IDE, browser brand, adapter id, or skill name.",
      "runtime_discovery": true,
      "adapter_boundary": "A host-specific adapter may expose measured capabilities and perform actions, but it cannot own policy, weaken validators, change terminal states, or become a prerequisite for the canonical workflow.",
      "required_terminal_states": ["PASS", "HANDOFF", "UNKNOWN", "FAIL"],
      "absence_rule": "The absence of one platform integration is not a blocker. After runtime discovery, the named blocker is NO_CAPABLE_ADAPTER with missing capabilities; unknown availability remains UNKNOWN and fails closed.",
      "identity_invariance": "Renaming an adapter without changing capabilities, state, evidence, risk, cost, or priority must not change its eligibility or score.",
      "secret_boundary": "Platform adaptation never authorizes password, MFA, CAPTCHA, cookie, profile, payment, recovery data, or secret values to enter agent-visible channels.",
      "allowed_platform_names": ["adapter inventory", "host adapter implementation", "dated evidence ledger", "compatibility alias"],
      "forbidden_platform_names": ["canonical selection predicate", "completion prerequisite", "validator exception", "authority expansion"]
    },
    "harness_contract": {
      "purpose": "Make the same FAMES policy loadable and mechanically auditable from every registered AI harness without allowing a harness name to own policy.",
      "execution_topology": "decentralized local execution and verification over one content-addressed contract; promotion remains single-writer so decentralization cannot become policy drift",
      "registered_population_source": "_registry/agent-surfaces.json",
      "required_surface_receipt_fields": ["surface_id", "discovery_path", "package_sha", "load_receipt", "behavior_probe", "local_verification", "read_back"],
      "selection_predicates": ["capability", "availability", "authority", "safety", "evidence_freshness", "cost"],
      "forbidden_predicates": ["harness_name", "product_name", "model_name", "vendor_name", "ide_name", "adapter_name", "skill_name"],
      "rename_invariance": "Changing only a surface or harness label must not change eligibility, route, terminal state, validator identity, or authority.",
      "coverage_rule": "A compliance claim covers exactly the surfaces registered and freshly read back. An unregistered, unloaded, stale, or unprobed harness is UNKNOWN, never implicitly compliant.",
      "authority_rule": "A harness may execute and verify locally but may not rewrite canonical policy, weaken a validator, or add authority. Canonical promotion retains exactly one writer.",
      "validator": "python scripts/fames_fleet.py validate-harness --input <record.json> --json"
    },
    "background_execution_contract": {
      "scope": "Every scheduled task, rider, daemon, watchdog, updater, and its full descendant process tree.",
      "rule": "Background execution must create zero visible console windows and steal focus zero times. This applies to every console-capable child, including git, package managers, shells, and helper CLIs.",
      "windows_required_controls": [
        "CREATE_NO_WINDOW",
        "STARTF_USESHOWWINDOW",
        "SW_HIDE",
        "shell_false",
        "interactive_prompts_disabled"
      ],
      "python_rule": "Use pythonw for a background Python entry point when available, but never treat pythonw as protection for child processes; each child still receives hidden creation flags.",
      "forbidden": [
        "DETACHED_PROCESS alone",
        "visible cmd or PowerShell fallback",
        "interactive credential prompt",
        "focus stealing",
        "claiming windowless operation without measured read-back"
      ],
      "manual_exception": "An operator-invoked foreground diagnostic may be visible only when it is explicitly classified manual_foreground and is not reachable from a scheduler.",
      "required_read_back": [
        "background",
        "full_descendant_policy_applied",
        "visible_window_count",
        "focus_steal_count",
        "interactive_prompts_disabled"
      ],
      "validator": "python scripts/fames_fleet.py validate-background --input <record.json> --json"
    },
    "hardware_compute_scheduler": {
      "profile": "references/hardware-compute.json",
      "policy_owner": "FAMES",
      "adapter_rule": "Host adapters measure and apply topology, affinity, priority, power throttling, and worker limits. They do not infer authority, importance, urgency, or service class.",
      "selection_predicates": [
        "measured_topology",
        "task_class",
        "verified_authority",
        "importance",
        "urgency",
        "deadline_evidence",
        "resource_pressure",
        "user_activity",
        "duration",
        "measured_scaling"
      ],
      "forbidden_predicates": [
        "agent_name",
        "harness_name",
        "model_name",
        "vendor_name",
        "seat_name",
        "process_name_alone"
      ],
      "reserve_rule": "Ordinary tasks preserve at least 2 measured P logical CPUs and 2 measured E logical CPUs for the operating system and interactive user.",
      "exclusive_rule": "Reserved CPUs may be used only with hardware.compute.exclusive authority, explicit operator-impact acknowledgement, duration at most 900 seconds, restore-on-exit, and exact read-back.",
      "topology_probe": "python scripts/fames_fleet.py measure-compute --workspace <root> --json",
      "planner": "python scripts/fames_fleet.py plan-compute --input <request.json> --json",
      "validator": "python scripts/fames_fleet.py validate-compute --input <record.json> --json"
    },
    "retention_rule": "Every file and process requires a verified caller or contract role; otherwise merge or remove it.",
    "minimize_order": ["physical_files", "executable_code", "execution_steps", "steady_state_resources"]
  },
  "fleet_topology": {
    "authority": {
      "node": "ai_darkhero",
      "contributor": "darkhero",
      "owns": ["canonical_generation", "promotion_gate"]
    },
    "followers": [
      {"node": "ai_scar3", "identity": "runtime_receipt"},
      {"node": "ai_altos", "identity": "runtime_receipt"}
    ],
    "follow_command": "python scripts/fames_fleet.py follow --workspace <root> --host <seat> --json",
    "converge_command": "python scripts/fames_fleet.py converge --workspace <root> --host <seat> --arm --json",
    "arm_command": "python scripts/fames_fleet.py arm --workspace <root> --host <seat> --apply --json",
    "transition": ["fetch_manifest", "verify_files", "verify_package", "atomic_activate", "write_receipt"],
    "self_convergence": {
      "principle": "A package that needs an external engine to update itself is not portable. The runner is part of the package: converge = follow + heartbeat + rider report, and arm registers the runner on the machine's own clock.",
      "rider": {
        "registry": "_registry/hosts.json",
        "host": "HubClock",
        "id": "fames-converge",
        "cadence": "15m",
        "priority": 90,
        "arming_engine": "_skill/engines/register-rider.py"
      },
      "heartbeat": "_registry/fames-converge/<host>.json",
      "heartbeat_fields": ["at", "outcome", "package_sha", "authority", "rider_state"],
      "degraded_path": "Where the arming engine is absent, arm prints the exact registration proposal and reports a named blocker instead of claiming a rider it did not create.",
      "disarm_respected": "--arm repairs a drifted registration but never re-adds a rider an operator removed.",
      "measured_by": ["C-CONVERGE-HEARTBEAT", "C-CONVERGE-ARMED"]
    },
    "convergence": {
      "online_bootstrapped": "one successful invocation",
      "offline_or_unbootstrapped": "UNKNOWN until first successful invocation",
      "proof": "local receipt and GitHub contributor manifest package_sha equal authority package_sha",
      "natural_events": {
        "boot": "HubClock autostart makes the in-package fames-converge rider eligible at the next 15 minute wall-clock boundary",
        "github_change": "authority manifest_sha or package_sha delta triggers verified atomic activation",
        "local_skill_change": "content fingerprint delta publishes the node contributor manifest",
        "network_recovery": "the next HubClock boundary retries after an unreachable GitHub event"
      },
      "online_lag_bound": "15m after HubClock is healthy and GitHub is reachable",
      "model_token_calls": 0,
      "steady_state_processes_added": 0
    },
    "capability_convergence": {
      "purpose": "Prove that darkhero, scar3, and altos expose the same required FAMES behaviours, not merely files with the same package hash.",
      "expected_hosts": ["darkhero", "scar3", "altos"],
      "generation_identity": ["package_sha", "capability_set_sha", "validator_set_sha"],
      "max_freshness_seconds": 3600,
      "transition": ["PUBLISH", "STAGE", "VERIFY", "ACTIVATE", "ATTEST", "RECONCILE", "COMMIT_CONVERGENCE"],
      "host_attestation_requires": ["package_identity", "runner_state", "capability_rows", "positive_control", "negative_control_rejected", "caller_backed", "fresh_observation", "evidence_refs"],
      "full_convergence_rule": "Full capability convergence requires the exact expected host set and a fresh PASS attestation for every required capability on every applicable host. Missing hosts, stale receipts, validator drift, unarmed runners, absent callers, failed negative controls, or package/capability/validator identity mismatch are UNKNOWN or FAIL and forbid a convergence claim.",
      "not_applicable_rule": "A capability may be NOT_APPLICABLE only with a registered false activation predicate and measured predicate evidence; host preference is not a predicate.",
      "repair_rule": "RECONCILE emits one host-and-capability-specific repair request. It never flattens partial success into fleet PASS.",
      "validator": "python scripts/fames_fleet.py validate-capability-sync --input <record.json> --json"
    },
    "peer_learning": {
      "followers_publish": ["evidence", "candidate_improvement"],
      "authority_promotes_after": ["FP", "SCF", "SEAL"],
      "followers_must_not": ["overwrite_canonical_generation"]
    }
  },
  "cognitive_operator_layer": {
    "purpose": "A provider-neutral reasoning quality layer for task completion, debugging, architecture, foresight and resource allocation. The labels name operations, never personality diagnoses or evidence.",
    "is_phase": false,
    "phase_relationship": "Runs inside task execution between MTM routing and SCF comparison; it does not alter FP -> MTM -> SCF -> AEX -> SEAL.",
    "selection_rule": "Choose the smallest pipeline matching the task class. Do not invoke every operator by habit.",
    "ut": {
      "label": "UT",
      "expanded_zh": "統一理論",
      "scope": "The project-internal umbrella for reasoning and engineering mechanisms that FAMES acquires from ai_ut.",
      "epistemic_boundary": "UT is the name of this internal unification layer, not a claim that it is established scientific truth. Each mechanism remains a hypothesis until its declared measurement passes.",
      "unclassified_mechanism_rule": "An ai_ut-derived function not yet enumerated remains a UT candidate. Route claim-checking or influence functions to interaction_integrity, but keep them UNKNOWN and non-SEAL-admissible until purpose, inputs, stop rule, evidence class, authority boundary, and validator are registered.",
      "includes": [
        "operators.Te",
        "operators.Ti",
        "operators.Ne",
        "operators.Ni",
        "operators.Si",
        "operators.Fe",
        "operators.Fi",
        "meticulousness_and_efficiency",
        "interaction_integrity",
        "delivery_integrity"
      ]
    },
    "operators": {
      "Ni": {
        "operation": "Converge heterogeneous evidence into one coherent, falsifiable model and expose the next discriminating test.",
        "strengthens": ["goal compression", "architecture coherence", "forward modelling", "priority selection"],
        "required_stop_rule": "model_and_discriminator",
        "failure_mode": "premature convergence that hides alternatives or treats a narrative as a fact"
      },
      "Te": {
        "operation": "Measure or change external state, then read it back against Verification.",
        "strengthens": ["task completion", "resource measurement", "reproduction", "deployment and guard installation"],
        "required_stop_rule": "measured_state_match",
        "failure_mode": "claiming completion because the work is understood, discussed or written while external state is unchanged"
      },
      "Ne": {
        "operation": "Discover alternative routes from the installed skill registry and fresh, identity-bound, in-scope ingest records. Allowed ingest origins are registered LINE intake and registered background web acquisition; raw search snippets and out-of-scope material are not evidence.",
        "strengthens": ["skill discovery", "ingest discovery", "error discovery", "architecture option search", "risk anticipation", "route discovery"],
        "required_stop_rule": "rank_saturation",
        "saturation_window": 3,
        "allowed_source_kinds": ["skill_registry", "line_ingest", "background_web_ingest"],
        "source_requirements": ["source_ref", "content_identity", "scope_match", "freshness_checked", "authority_checked"],
        "failure_mode": "unbounded ideation that consumes resources without changing a decision"
      },
      "Ti": {
        "operation": "Formalize candidates as invariants, contracts and a bounded counterexample search.",
        "strengthens": ["root-cause isolation", "interface design", "consistency checking", "constraint enforcement"],
        "required_stop_rule": "invariants_and_counterexamples",
        "failure_mode": "internally elegant analysis with no falsifiable contract or stopping budget"
      },
      "Si": {
        "operation": "Retrieve a versioned, measured, caller-backed known structure and its previously passing application constraints; never turn familiarity, repetition, or memory alone into evidence.",
        "strengthens": ["stable reuse", "known-structure recovery", "non-regression", "application continuity"],
        "required_stop_rule": "known_structure_reproduced",
        "failure_mode": "treating stale, unversioned, unverified, or merely familiar material as a reusable known"
      },
      "Fe": {
        "operation": "Bound the inter-agent context: actors, commitments, authority, audience, repair attempts, and observable discrepancies. This is an audit-only context operator and does not actuate another agent.",
        "strengthens": ["interaction context", "commitment comparison", "coordination integrity", "repair routing"],
        "required_stop_rule": "interaction_context_bounded",
        "failure_mode": "inferring intent from tone, social expectation, confidence, or disagreement"
      },
      "Fi": {
        "operation": "Compare one actor's claims and declared commitments against measured evidence and explicit invariants. It may classify support or contradiction but cannot establish deception intent without direct measured knowledge-and-deliberation evidence.",
        "strengthens": ["claim integrity", "commitment consistency", "evidence conflict detection", "intent-boundary preservation"],
        "required_stop_rule": "claim_integrity_bounded",
        "failure_mode": "calling an error, contradiction, omission, or value conflict a lie without direct measured intent evidence"
      }
    },
    "pipelines": {
      "completion": [
        {"operator": "Ni", "role": "synthesize_goal_model"},
        {"operator": "Te", "role": "apply_and_verify"}
      ],
      "debugging": [
        {"operator": "Ne", "role": "generate_failure_hypotheses"},
        {"operator": "Ti", "role": "isolate_invariant_violation"},
        {"operator": "Te", "role": "reproduce_fix_and_verify"}
      ],
      "architecture": [
        {"operator": "Ne", "role": "enumerate_viable_shapes"},
        {"operator": "Ti", "role": "define_invariants_and_boundaries"},
        {"operator": "Ni", "role": "select_coherent_system_model"},
        {"operator": "Te", "role": "prototype_and_measure"}
      ],
      "foresight": [
        {"operator": "Ni", "role": "form_forward_model"},
        {"operator": "Ne", "role": "branch_counterfactual_failures"},
        {"operator": "Ti", "role": "reject_inconsistent_branches"},
        {"operator": "Te", "role": "install_probe_or_guard"}
      ],
      "resource_allocation": [
        {"operator": "Te", "role": "measure_budget_and_capacity"},
        {"operator": "Ne", "role": "enumerate_routes"},
        {"operator": "Ti", "role": "enforce_constraints"},
        {"operator": "Ni", "role": "rank_by_goal_value"},
        {"operator": "Te", "role": "apply_route_and_verify_cost"}
      ],
      "known_structure_application": [
        {"operator": "Si", "role": "retrieve_verified_known_structure"},
        {"operator": "Ti", "role": "restate_invariants_and_application_bounds"},
        {"operator": "Te", "role": "apply_known_structure_and_read_back"}
      ],
      "agent_integrity": [
        {"operator": "Fe", "role": "bound_interaction_context"},
        {"operator": "Fi", "role": "assess_claim_and_commitment_integrity"}
      ]
    },
    "trace_contract": {
      "required_trace_fields": ["task_class", "evidence_class", "stages"],
      "required_stage_fields": ["operator", "role", "input_ref", "output_ref", "stop"],
      "seal_admissible_evidence": ["measured"],
      "prediction_rule": "A forward branch is a hypothesis until Te installs a probe or guard and measured evidence resolves it.",
      "stable_application_rule": "Si retrieves a known structure, Ti revalidates its invariants, and Te applies and reads it back. Te residuals return only to the next FAMES run; Si cannot silently promote new memory.",
      "integrity_rule": "Fe and Fi may detect context or evidence contradictions, but a deception verdict still requires direct measured proof of knowledge and deliberate misrepresentation.",
      "validator": "python scripts/fames_fleet.py validate-cognitive --input <trace.json> --json",
      "unknown_rule": "An unknown task class, missing operator, wrong order, unmet stop condition or unreadable policy is UNKNOWN or FAIL and cannot seal."
    },
    "meticulousness_and_efficiency": {
      "status": "UT project-internal engineering hypothesis",
      "purpose": "Measure early risk, accounting conservation, loop escape, mutation bounds, negative interactions, leading indicators, representation fit, inverse diagnosis, and epistemic admissibility without treating cognitive labels as personality science.",
      "is_phase": false,
      "validator": "python scripts/fames_fleet.py validate-cognitive --input <trace.json> --json",
      "unknown_rule": "Undefined units, malformed types, missing fields, stale series, exceptions, and unknown mechanisms are UNKNOWN and fail closed.",
      "mechanisms": {
        "stability_margin": {
          "required": ["unit", "margin", "trend", "measured_at", "leading_indicator", "warning_threshold", "trend_limit", "status"],
          "rule": "PASS is current only; a near boundary or rapidly shrinking measured margin is warning, while an undefined unit is UNKNOWN."
        },
        "conservation_ledger": {
          "cost_fields": ["tokens", "tool_calls", "wall_ms", "external_mutations"],
          "rule": "Each transition accounts for costs, evidence, authority, and an explicit residual; authority and evidence cannot expand without measured input."
        },
        "loop_escape": {
          "escape_actions": ["discriminating_test", "representation_change", "blocked_unknown"],
          "rule": "Three consecutive attempts with no external or discriminating delta require an escape action; productive repetitions remain valid."
        },
        "grip_guard": {
          "activation": "R2 or R3 mutation after internal analysis",
          "rule": "High-impact mutation requires a bounded transaction, rollback and read-back, or a measured emergency recovery drill."
        },
        "destructive_interference": {
          "residuals": ["R_CAPABILITY", "R_OPERABILITY", "R_COMPLEXITY"],
          "rule": "Every new capability must retain a passing caller-backed core probe before and after installation."
        },
        "bifurcation_foresight": {
          "series": ["margin", "retry_rate", "queue_depth", "oscillation_rate", "stale_evidence_age"],
          "rule": "Before crossing, output is forecast/hypothesis; after a measured crossing it is a measured conclusion; stale series is UNKNOWN."
        },
        "basis_alignment": {
          "metrics": ["conversion_steps", "tool_calls", "bytes_context", "wall_ms"],
          "operator_route": ["Ne", "Ti", "Ni", "Te"],
          "rule": "Use replayable measured units and reject a selected representation dominated by a cheaper compatible route; unitless aggregate scores are forbidden."
        },
        "inverse_diagnosis": {
          "status": "engineering supplement; not an original-theory claim",
          "mapping": {
            "external_change_missing": {"stage": "Te", "test": "read_back_external_state"},
            "architecture_contradiction": {"stage": "Ti", "test": "bounded_counterexample"},
            "premature_single_route": {"stage": "Ne/Ni", "test": "rank_saturation_and_discriminator"},
            "unverifiable_prediction": {"stage": "Ni/Te", "test": "install_decisive_probe"}
          }
        },
        "epistemic_layers": {
          "layers": ["source/measured", "model/hypothesis", "draft/unverified"],
          "seal_admissible": ["source/measured"],
          "rule": "Tone never promotes a claim; SEAL accepts only the evidence class required by the goal."
        }
      }
    },
    "interaction_integrity": {
      "status": "UT project-internal interaction contract",
      "purpose": "Assess counterpart claims without mind-reading and align agent compliance through transparent, authorized reasons rather than manipulation.",
      "is_phase": false,
      "validator": "python scripts/fames_fleet.py validate-cognitive --input <trace.json> --json",
      "claim_integrity": {
        "operator_roles": {"Fe": "bound interaction context and observable commitments", "Fi": "compare claims and commitments with measured evidence"},
        "evidence_statuses": ["matches", "conflicts", "insufficient"],
        "claim_states": ["SUPPORTED", "CONTRADICTED", "UNKNOWN"],
        "mapping": {
          "matches": "SUPPORTED",
          "conflicts": "CONTRADICTED",
          "insufficient": "UNKNOWN"
        },
        "deception_rule": "Fe and Fi may jointly expose a contradiction, but contradiction is not proof of lying. Deception may be SUPPORTED only by direct measured evidence that the actor knew the claim was false and deliberately represented it as true; otherwise intent is UNKNOWN.",
        "evidence_reproduction_rule": "The validator checks record consistency, not the truth of an evidence URI. Every load-bearing evidence reference must be independently reproduced before SEAL.",
        "unknown_rule": "Missing, stale, indirect, modelled, or merely asserted evidence cannot establish deception."
      },
      "delivery_integrity": {
        "purpose": "Detect unsupported completion, evasion, and hand-waving as evidence defects without inferring intent.",
        "required_claim_fields": ["claim_id", "claim_type", "subject", "scope", "action_state", "artifact_identity", "evidence_class", "evidence_refs", "status", "residuals"],
        "claim_types": ["fixed", "deployed", "verified", "numeric", "exhaustive", "synchronized", "reviewed"],
        "action_states": ["proposed", "requested", "implemented", "built", "deployed", "read_back"],
        "defect_classes": ["evidence_substitution", "scope_inflation", "identity_drift", "aggregation_masking", "negative_result_suppression", "action_state_laundering", "proxy_metric_substitution", "stale_evidence", "failure_path_omission"],
        "scope_rule": "A claim over all, every, complete, fleet, or exhaustive scope requires a measured population identity, expected count, covered count, per-item verdicts or an equivalent deterministic coverage proof, and zero hidden UNKNOWN items. Sampling cannot support an exhaustive claim.",
        "numeric_rule": "A numeric claim records unit, claimed value, measured value, delta, source, and measurement time. A total, proxy, or differently scoped metric cannot substitute for the named measure.",
        "fixed_rule": "A fixed claim requires the same reproducer to fail before and pass after on the claimed artifact identity, plus affected non-regression evidence.",
        "deployment_rule": "A deployed or synchronized claim requires source revision, deployed version, destination read-back, behaviour-specific verification, and rollback identity. Build success, HTTP 200, push success, or a sent request is not a substitute.",
        "failure_rule": "Negative paths must distinguish failure from empty data, fake zero, blank UI, timeout, or infinite loading. Omitted required failure probes keep the claim UNKNOWN.",
        "intent_boundary": "These defects can contradict a delivery claim but cannot establish deception intent without the separate direct measured intent evidence required by claim_integrity."
      },
      "repair_guidance": {
        "required_fields": ["request_id", "claim_id", "shared_goal", "failed_invariant", "observed_evidence", "missing_proof", "smallest_next_test", "acceptance_condition", "authority_boundary", "attempt", "max_attempts"],
        "rule": "Guide the counterpart with the exact failing invariant, smallest discriminating test, and an allowed equivalent proof. Revalidate the frozen goal and validator after revision.",
        "stop_rule": "Stop with HANDOFF or UNKNOWN after the declared budget or after three attempts with neither artifact identity change nor discriminating evidence. Never weaken the validator or erase a negative result."
      },
      "compliance_alignment": {
        "allowed_methods": ["shared_goal", "evidence", "constraint_explanation", "consequence_explanation", "bounded_options", "structured_repair"],
        "forbidden_methods": ["deception", "coercion", "threat", "impersonation", "hidden_pressure", "exploit_vulnerability", "authority_expansion"],
        "required_properties": ["authorized", "goal_explicit", "constraints_explicit", "evidence_refs", "counterparty_choice_preserved", "authority_conserved"],
        "conflict_rule": "An instruction conflicting with higher-priority policy, safety, or user authority must be refused rather than persuaded into execution.",
        "success_rule": "Alignment succeeds when the counterpart accepts, repairs, or gives an evidence-backed refusal while authority and choice remain intact; obedience alone is not the metric."
      },
      "unknown_rule": "Unknown evidence identity, authority, intent, method, scope coverage, artifact identity, action state, or constraint conflict is UNKNOWN and fails closed."
    }
  },
  "autonomic_lifecycle": {
    "purpose": "Make FAMES a bounded, evidence-driven process that continuously absorbs candidate learning, advances safe work, reviews itself, evolves on measured residuals, and synchronizes verified generations without expanding authority.",
    "is_phase": false,
    "relationship": "This lifecycle connects repeated FAMES runs; it does not add a sixth phase. Every mutation inside DRIVE or EVOLVE still executes FP -> MTM -> SCF -> AEX -> SEAL.",
    "cycle": ["SENSE", "ABSORB", "TRIAGE", "DRIVE", "REVIEW", "EVOLVE", "SYNC", "OBSERVE", "SENSE"],
    "invariant": "Automation increases persistence, not authority.",
    "execution_boundary": "The lifecycle advances automatically only through registered event sources, deterministic engines, and writable controller channels. Without an actuator it may validate, classify, and emit a handoff, but must not claim that another actor was driven or repaired.",
    "stages": {
      "SENSE": "Read fresh receipts, handoffs, residuals, heartbeats, installed-skill manifests, and identity-bound candidate material by identity rather than importing an entire conversation as truth.",
      "ABSORB": "Let Ne discover only registered skills and fresh in-scope ingest records from registered LINE intake or background web acquisition. Extract the smallest structural claims; preserve provenance, academic citations when applicable, IP/disclosure class, hypotheses, contradictions, and honest negative results. Proprietary raw material remains local.",
      "TRIAGE": "Separate mechanical work, judgment, and authority decisions. Owner-only decisions become evidence-backed handoffs; safe non-conflicting branches continue when isolation is verified.",
      "DRIVE": "Use an available controller channel to issue structured repair requests and re-run a frozen validator within a bounded attempt budget. Without a controller, emit a handoff and remain UNKNOWN.",
      "REVIEW": "Review artifacts rather than builder narrative through the first available independent lane; reproduce evidence and retain negative results.",
      "EVOLVE": "Activate AEX only on a measured residual. Promote only after a measured trial, accepted review, authority check, semantic version/generation change, and non-regression evidence.",
      "SYNC": "Publish a content-addressed package and require atomic, idempotent follower read-back. A stale or unreachable follower remains named UNKNOWN and forbids a full-convergence claim.",
      "OBSERVE": "Run self-check and liveness probes, record remaining residuals, and enqueue the next discriminating action so the loop closes on measured state."
    },
    "triage_lanes": ["mechanical", "judgment", "authority"],
    "safe_isolation": ["read_only", "path_limited", "independent_worktree", "bounded_transaction", "not_applicable"],
    "review_lanes": ["dedicated_readonly", "independent_agent", "fresh_evidence_pass"],
    "terminal_states": ["PASS", "HANDOFF", "UNKNOWN", "FAIL"],
    "stop_rules": [
      "PASS requires the frozen validator and external read-back to pass.",
      "A missing controller, owner-only decision, access block, or unverifiable evidence stops mutation and emits HANDOFF or UNKNOWN.",
      "Do not repeat after the attempt budget, or after three attempts with neither artifact-identity change nor discriminating evidence.",
      "Never edit the validator or erase a negative result merely to obtain PASS."
    ],
    "record": {
      "required_fields": ["cycle_id", "sources", "triage", "drive", "review", "evolution", "sync", "observation", "authority_before", "authority_after"],
      "ip_classes": ["public", "internal", "restricted", "proprietary"],
      "disclosure_classes": ["publishable", "paste_safe", "local_only"],
      "unknown_rule": "Missing source identity, disclosure/IP classification, routing evidence, controller state, review identity, promotion gate, sync read-back, or observation feedback is UNKNOWN and fails closed."
    },
    "validator": "python scripts/fames_fleet.py validate-autonomic --input <cycle.json> --json"
  },
  "external_learning": {
    "purpose": "Governs information acquired from OUTSIDE the fleet that proposes an improvement to the fleet. Turns 'I read something useful' into work with the same completion truth as any other FAMES work.",
    "is_phase": false,
    "lane_rule": "This is a lane that feeds AEX, not a sixth phase. execution_order is unchanged. A promoted candidate is an adaptation and is judged by AEX, SCF and SEAL like any other adaptation.",
    "activation_predicate": "material originating outside the fleet is used to justify a change to fleet canon, tooling or routing",
    "stages": ["ACQUIRE", "UNDERSTAND", "CLAIM", "TRIAL", "PROMOTE"],
    "discovery_sources": {
      "allowed": ["skill_registry", "line_ingest", "background_web_ingest"],
      "scope_rule": "Ne may discover only records whose content identity, source reference, freshness, authority and user-scope match are explicit. A search result is a pointer to acquire, never a fact.",
      "line_rule": "LINE material must already exist as a persisted, identified ingest receipt; unavailable chat history remains unavailable.",
      "background_web_rule": "Background web material must come from a registered acquisition event and retain source URI, acquisition time and content identity; autonomous unbounded crawling is forbidden."
    },
    "stage_contract": {
      "ACQUIRE": {
        "owns": ["retrieval of bits from a channel", "provenance", "content identity"],
        "must_not_own": ["interpretation"],
        "cost_rule": "route order is cost order: publisher-supplied text (0 quota) -> local model (0 quota, material never leaves the machine) -> metered API",
        "authority_rule": "A login wall, paywall, bot check or rate block is a named blocker reported to the operator, never a puzzle to solve. Acquisition never creates an account and never enters a credential."
      },
      "UNDERSTAND": {
        "owns": ["bits -> facts by declared capability"],
        "must_not_own": ["the artifact", "the verdict"],
        "capability_rule": "A stage declares a capability (asr.*, vision.*, text.*), never a vendor; the vendor is a data-driven junction chosen by scheduling and rotation.",
        "separation_rule": "The model supplies language, not facts. A fact is measured by a deterministic instrument or it is not a fact."
      },
      "CLAIM": {
        "owns": ["what the source asserts, attributed to the source"],
        "must_not_own": ["truth"],
        "rule": "A source assertion is recorded as a claim with its trust class and its own wording bounded to the minimum needed to test it. Restating a claim never promotes it to fact.",
        "academic_rule": "An academic source requires both in-text citations attached to claim ids and a reference list. Every in-text citation id must resolve to exactly one reference-list entry carrying authors, year, title, container and a persistent identifier; uncited academic claims are UNKNOWN."
      },
      "TRIAL": {
        "owns": ["a bounded local test of the claim against this fleet"],
        "must_not_own": ["canon"],
        "rule": "The trial's Verification is written before the trial runs, exactly as in FP. A claim that cannot be given a Verification is rejected as untestable, not adopted on plausibility.",
        "already_covered_rule": "Before trialling, check whether existing canon already implements the claim; already_covered is a first-class verdict and costs nothing."
      },
      "PROMOTE": {
        "owns": ["canon change"],
        "must_not_own": ["silent adoption"],
        "gate": "authority node only, after FP, SCF and SEAL — the same gate as fleet_topology.peer_learning; followers may publish an ingest record as candidate_improvement but must not promote it."
      }
    },
    "trust_classes": {
      "measured": "produced by a deterministic local instrument (ffprobe duration, sha256, exit status, HTTP status). Admissible as fact.",
      "modelled": "produced by a model reading the material (ASR, OCR, translation, summary). Admissible as a rendering of the source, never as a fact about the world.",
      "asserted": "the source's own statement. Admissible only as a claim attributed to that source."
    },
    "admissibility_rule": "Only measured items may be cited as SEAL evidence. A modelled or asserted item enters as a candidate and requires a measured verification of its own before any phase may go PASS on it.",
    "record": {
      "path": "_registry/fames-evidence/ingest-<stamp>-<slug>.json",
      "required_fields": ["source_uri", "source_kind", "source_channel", "scope", "acquired_at", "acquisition_route", "quota_cost", "content_identity", "trust_class_map", "claims", "verdict", "promoted"],
      "source_kinds": ["skill", "academic", "web", "line", "other"],
      "source_channels": ["skill_registry", "line_ingest", "background_web_ingest", "manual"],
      "claim_fields": ["id", "claim", "trust_class", "verdict", "why"],
      "academic_citation_fields": ["style", "in_text", "references"],
      "in_text_fields": ["citation_id", "claim_ids", "locator"],
      "reference_fields": ["citation_id", "authors", "year", "title", "container", "persistent_id"],
      "verdict_values": ["adopt", "adapt", "reject", "already_covered", "UNKNOWN"],
      "forbidden_material": ["raw credential", "token", "cookie", "session material", "reproduced copyrighted body"],
      "unknown_rule": "A record missing provenance, content identity, route or a per-claim verdict is UNKNOWN and fails closed; UNKNOWN never promotes."
    },
    "validator": "python scripts/fames_fleet.py validate-ingest --input <ingest.json> --json",
    "non_goals": ["autonomous crawling", "account creation", "bypassing an access control", "retaining material the source licenses against"],
    "does_not_expand_authority": true
  },
  "production_delivery_assurance": {
    "profile": "references/production-web-delivery.json",
    "activation": "production website, Worker, API, OAuth, webhook, database, analytics, or other externally observable delivery",
    "risk_floor": "R2 for deployment or external-state mutation",
    "rule": "Use exhaustive scope ledgers, claim-versus-measurement tables, failure-path probes, bounded resource accounting, isolated mutation, production version read-back, rollback identity, and per-item PASS/FAIL/UNKNOWN. Project-specific counts are regression baselines, never universal FAMES constants."
  },
  "distribution": {
    "mode": "content-addressed-github-bundle",
    "canonical_package": "_skill/fleet-skills/fames",
    "bundle_manifest": "_skill/fleet-skills/fames/bundle-manifest.json",
    "bundled_protocol": "_skill/fleet-skills/fames/references/protocols/fames-protocol.json",
    "package_verifier": "_skill/fleet-skills/fames/scripts/fames_fleet.py",
    "carrier_repo": "https://github.com/scss1199/ai_fleet_skills.git",
    "host_receipt": "contributors/<host>/manifest.json#skills[name=fames].package_sha",
    "unknown_rule": "A missing receipt, stale carrier manifest, unreadable bundle, or package hash mismatch is UNKNOWN and fails closed.",
    "event_runner": "in-package HubClock rider fames-converge -> scripts/fames_fleet.py converge (self-contained; no hub engine on the update path)",
  "event_runner_legacy": "hub-only convenience path: fleet-skill-pulse -> fleet-skill-sync-tick.py -> mtm-fleet-skill-github.py tick. Publishes contributor manifests; not required for a follower to update.",
    "no_change_cost": "one authority manifest fetch for followers; package files are not fetched when verified package_sha already matches"
  }
}
